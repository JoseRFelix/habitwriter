import { DefaultTheme } from 'styled-components'

const white: DefaultTheme = {
    colors: {
        main: 'cyan',
        secondary: 'magenta',
        background: '#ffffff'
    },
}

const themes = { white };

export default themes;